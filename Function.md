# Function

| function                             | 반환                | 기능                                                         | 참고                           |
| ------------------------------------ | ------------------- | ------------------------------------------------------------ | ------------------------------ |
| `print()`                            |                     | 피연산자를 출력한다.                                         | 4.1                            |
| `type()`                             |                     | 피연산자 객체의 타입을 반환한다.                             | 5.1.6                          |
| `int()`                              |                     | 피연산자를 정수로 바꾸어 반환한다.                           | 5.1.5                          |
| `float()`                            |                     | 피연산자 숫자, 계산식, 실수 또는 정수로 된 문자열을 실수로 반환한다. | 5.2.2                          |
| `complex(실수부, 허수부)`            |                     | 피연산자를 `(실수부+허수부j)` 튜플로 만들어 반환한다.        |                                |
| `bool()`                             |                     | 피연산자를 `True` 혹은 `False`로 반환한다.                   | 8.2                            |
| `divmod(분모, 분자)`                 |                     | 피연산자를 `(몫, 나머지)` 튜플로 만들어 반환한다.            | 5.1.6                          |
| `del a`<br />`del(a)`                |                     | 피연산자 `a`를 삭제한다.                                     | 6.1.2<br />11.3.5              |
| `input()`                            |                     | 피연산자를 사용자 입력을 받기 전 프롬프트로서 띄워준다. 사용자의 입력을 반환한다. | 6.3.1                          |
| `id()`                               |                     | 피연산자 객체의 메모리 주소를 반환한다.                      | 8.1.5                          |
| `range()`                            |                     | 피연산자가 지정한 범위 안 숫자를 `range` 객체로 반환한다.    | 10.1.3<br />16.1               |
| `list()`                             |                     | iterable한 object를 피연산자로 받아 리스트를 만들어 반환한다. | 10.1                           |
| `tuple()`                            |                     | iterable한 object를 피연산자로 받아 튜플을 만들어 반환한다.  | 10.2                           |
| `len(반복가능한객체)`                | `int`               | `반복가능한객체`의 요소의 개수를 반환한다.                   | 22.1.2<br />11.2.1<br />12.2.1 |
| `deque()`                            |                     | 반복가능한 객체를 피연산자로 받아 덱으로 만들어 반환한다.    | 22.1.8                         |
| `reversed()`                         | `reversed object`   | 시퀀스 객체인 피연산자 속 요소의 순서를 뒤집어 `reversed object` 타입으로 반환한다. | 16.2                           |
| `sorted()`                           |                     | 시퀀스 객체 속 요소를 정렬하여 **새로운 리스트로 반환한다.** | 22.1.12                        |
| `dict()`                             |                     | 딕셔너리를 만들어 반환한다.                                  | 12.1.3                         |
| `zip()`                              |                     | 피연산자로 `zip` 객체를 만들어 반환한다.                     | 12.1.4                         |
| `값 in 시퀀스 객체`                  |                     | 값이 시퀀스 객체 안에 있는지 확인하고 있으면 `True`를 반환한다. | 11.1.1                         |
| `값 not in 시퀀스 객체`              |                     | 값이 시퀀스 객체 안에 있는지 확인하고 없으면 `True`를 반환한다. |                                |
| `키 in 딕셔너리`                     |                     | 딕셔너리에 키가 있는지 확인하고, 있으면 `True`를 반환한다.   | 12.2.2                         |
| `키 not in 딕셔너리`                 |                     | 딕셔너리에 키가 있는지 확인하고, 없으면 `False`를 반환한다.  |                                |
| `help(함수)`                         | `NoneType`          | `함수`의 이름, 매개변수, 독스트링을 반환한다.                | 29                             |
| `dir(객체)`                          | `list`              | `객체`의 메서드 이름들을 리스트를 반환한다.                  | 39                             |
| `iter(호출가능한객체, 반복을끝낼값)` | `collable_iterator` |                                                              | 39.4                           |
| `ord(문자)`                          | `int`               | `문자`의 유니코드 값을 반환한다.                             | E11.5                          |
| `map(lambda, 반복가능한객체)`        | `map`               | `반복가능한객체`를 순회하여 각각 `lambda`를 적용한 결과를 반환한다. | 32                             |
| `filter(lambda, 반복가능한객체)`     | `filter`            | `반복가능한객체`를 순회하여 `lambda` 함수의 반환값이 `True`인 경우만을 반환한다. |                                |
| `enumerate(반복가능한객체)`          | `enumerate`         | `반복가능한객체`를 `enumerate` 타입으로 변환하여 반환한다.   | E4                             |



## 시퀀스 객체 Method

| function              | 기능                                     | 참고    |
| --------------------- | ---------------------------------------- | ------- |
| `index()`             | 지정한 값이 있는 인덱스를 찾아 반환한다. | 22.1.9  |
| `count()`             | 지정한 값이 몇 개 있는지 세어 반환한다.  | 22.1.10 |
| `__getitem(인덱스)__` | 지정한 인덱스의 값을 가져온다.           | 11.3    |



## 반복 가능한 객체 Method

| function              | 반환       | 기능                                                 | 참고   |
| --------------------- | ---------- | ---------------------------------------------------- | ------ |
| `__iter__()`          | 이터레이터 | 반복 가능한 객체의 이터레이터를 반환한다.            | 39     |
| `min(반복가능한객체)` |            | `반복가능한객체`의 요소 중 가장 작은 수를 반환한다.  | 22.4.1 |
| `max(반복가능한객체)` |            | `반복가능한객체`의 요소 중 가장 큰 수를 반환한다.    |        |
| `sum(반복가능한객체)` |            | `반복가능한객체`의 모든 요소를 더한 합계를 반환한다. | 22.4.2 |



## 이터레이터 메서드

| function     | 반환              | 설명                                                         | 참고 |
| ------------ | ----------------- | ------------------------------------------------------------ | ---- |
| `__next__()` | 이터레이터의 요소 | 이터레이터의 요소를 호출할 때마다 차례로 반환하고, 더이상 반환할 요소가 없으면 `StopIteration` 예외를 발생시킨다. | 39   |



## random

| function              | 기능                                                      | 참고 |
| --------------------- | --------------------------------------------------------- | ---- |
| `random.random()`     | 난수를 생성하여 반환                                      | 17.2 |
| `random.randint(a,b)` | 피연산자에 대해 `a~(b-1)`까지의 정수 난수를 생성하여 반환 |      |
| `random.choice()`     | 피연산자 시퀀스 객체에서 요소를 무작위로 선택하여 반환    |      |



## str Method

| function                  | 반환   | 기능                                                         | 참고    |
| ------------------------- | ------ | ------------------------------------------------------------ | ------- |
| `split('기준 문자열')`    | `list` | 피연산자를 기준으로 문자열을 분리하고, 리스트로 반환한다.    | 6.4     |
| `splitlines()`            | `list` | 문자열을 `\n` 단위로 끊어 문자열의 리스트로 반환한다.        |         |
| `encode(인코딩)`          |        |                                                              | 11.2.3  |
| `replace(타겟, 새문자열)` | `str`  | 문자열 안의 `타겟`을 `새문자열`로 변경한 새로운 문자열을 반환 | 24.1.1  |
| `translate(딕셔너리)`     | `dict` | 문자열 안의 문자들을 `딕셔너리`를 통해 치환하여 새로운 문자열을 반환 | 24.1.2  |
| `join(문자열리스트)`      | `str`  | `문자열리스트`의 문자열 요소들을 문자열로 연결하여 새로운 문자열을 반환 | 24.1.4  |
| `upper()`                 | `str`  | 문자열의 모든 문자를 대문자로 바꾸어 새로운 문자열을 반환    | 24.1.5  |
| `lower()`                 | `str`  | 문자열의 모든 문자를 소문자로 바꾸어 새로운 문자열을 반환    | 24.1.6  |
| `swapcase()`              | `str`  | 문자열 속 대문자는 소문자로, 소문자는 대문자로 바꾸어 반환한다. |         |
| `title()`                 | `str`  | 각 단어의 앞 글자만 대문자로 바꾸어 반환한다.                |         |
| `lstrip()`                | `str`  | 문자열의 왼쪽에 있는 공백을 모두 삭제한 새로운 문자열을 반환 | 24.1.7  |
| `rstrip()`                | `str`  | 문자열의 오른쪽에 있는 공백을 모두 삭제한 새로운 문자열을 반환 | 24.1.8  |
| `strip()`                 | `str`  | 문자열의 양쪽에 있는 연속된 모든 공백을 삭제한 새로운 문자열을 반환 | 24.1.9  |
| `lstrip()`                | `str`  | 문자열의 왼쪽에 있는 `문자열`의 각각의 문자들을 삭제         | 24.1.10 |
| `rstrip(문자열)`          | `str`  | 문자열의 오른쪽에 있는 `문자열`의 각각의 문자들을 삭제       | 24.1.11 |
| `strip(문자열)`           | `str`  | 문자열의 양쪽에 있는 `문자열`의 각각의 문자들을 삭제         | 24.1.12 |
| `ljust(길이)`             | `str`  | 총 `길이`에서 문자열 왼쪽 정렬하여 새로운 문자열을 반환      | 24.1.13 |
| `rjust(길이)`             | `str`  | 총 `길이`에서 문자열 오른쪽 정렬하여 새로운 문자열을 반환    | 24.1.14 |
| `center(길이)`            | `str`  | 총 `길이`에서 문자열 가운데 정렬하여 새로운 문자열을 반환    | 24.1.16 |
| `zfill(길이)`             | `str`  | 총 `길이`에서 문자열을 왼쪽 정렬한 후, 남는 길이에 0을 채워 새로운 문자열을 반환 | 24.1.17 |
| `find(문자열)`            | `int`  | 문자열의 왼쪽에서부터 `문자열`을 찾아 '문자열'의 첫번째 문자의 인덱스를 반환 | 24.1.18 |
| `rfind(문자열)`           | `int`  | 문자열의 왼쪽에서부터 `문자열`을 찾아 '문자열'의 첫번째 문자의 인덱스를 반환 | 24.1.19 |
| `index(문자열)`           | `int`  | `find(문자열)`과 같으나, 문자열을 찾지 못하면 오류를 발생시킴 | 24.1.20 |
| `rindex(문자열)`          | `int`  | `rfind(문자열)`과 같으나, 문자열을 찾지 못하면 오류를 발생시킴 | 24.1.21 |
| `count(문자열)`           | `int`  | 문자열 안의 `문자열`이 몇 개 있는지 세어 반환한다.           | 24.1.22 |
| `format()`                | `str`  | 인덱스나 키워드를 통해 구성한 새로운 문자열을 반환한다.      | 24.2.6  |
| `startswith(문자열)`      | `bool` | 문자열이 `문자열`로 시작하면 `True`를 반환한다.              |         |
| `endswith(문자열)`        | `bool` | 문자열이 `문자열`로 끝면 `True`를 반환한다.                  |         |
| `isdigit()`               | `bool` | 문자열이 digit이면 `True`를 반환한다.                        |         |
| `isalpha()`               | `bool` | 문자열이 알파벳으로만 이루어져있으면 `True`를 반환한다.      |         |
| `isalnum()`               | `bool` | 문자열이 알파벳과 숫자로만 이루어져 있으면 `True`를 반환한다. |         |
| `islower()`               | `bool` | 문자열이 소문자로만 이루어져 있으면 `True`를 반환한다.       |         |
| `isupper()`               | `bool` | 문자열이 대문자로만 이루어져있으면 `True`를 반환한다.        |         |
| `isspace()`               | `bool` | 문자열이 공백 문자로만 이루어져있으면 `True`를 반환한다.     |         |



## str class method

| function                            | 반환   | 기능                                                         | 참고   |
| ----------------------------------- | ------ | ------------------------------------------------------------ | ------ |
| `str.maketrans(키문자열, 값문자열)` | `dict` | `키문자열`의 각각의 문자를 `값문자열`의 각각의 문자와 짝지어 딕셔너리를 만들어 반환 | 24.1.2 |
|                                     |        |                                                              |        |



## list Method

| function                   | 반환   | 기능                                                         | 참고    |
| -------------------------- | ------ | ------------------------------------------------------------ | ------- |
| `append()`                 |        | 리스트에 요소를 추가한다.                                    | 22.1.2  |
| `extend()`                 |        | 리스트에 피연산자 리스트를 연결한다.                         | 22.1.4  |
| `insert(인덱스, 요소)`     |        | 지정한 인덱스에 요소를 추가한다.                             | 22.1.5  |
| `pop()`<br />`pop(인덱스)` |        | 리스트에서 마지막 요소를 삭제하거나, 지정한 `인덱스`의 요소를 삭제한 후 **삭제한 요소를 반환한다**. | 22.1.7  |
| `remove(값)`               |        | 지정한 `값`을 리스트에서 찾아 삭제한다.                      | 22.1.8  |
| `index(값)`                | `int`  | 지정한 `값`의 인덱스를 리스트에서 찾아 반환한다.             | 22.1.9  |
| `count(값)`                | `int`  | 지정한 `값`의 개수를 리스트에서 세어 반환한다.               | 22.1.10 |
| `reverse()`                |        | 리스트 속 요소의 순서를 뒤집는다.                            | 22.1.11 |
| `sort()`                   |        | 리스트 속 요소를 정렬한다.                                   | 22.1.12 |
| `clear()`                  |        | 리스트의 모든 요소를 삭제한다.                               | 22.1.13 |
| `copy()`                   | `list` | 원본 리스트를 복사한 새로운 객체를 반환한다.                 | 22.2.2  |



## dict Method

| 함수             | 반환                 | 기능                                          | 참고   |
| ---------------- | -------------------- | --------------------------------------------- | ------ |
| `setdefault(키)` |                      | 딕셔너리에 `키`를 추가한다.                   | 25.1.2 |
| `update()`       |                      | 딕셔너리의 키에 해당하는 값을 수정한다.       | 25.1.3 |
| `pop(키)`        | `키`에 해당하는 `값` | 딕셔너리에서 `키`를 삭제하고 `값`을 반환한다. | 25.1.4 |
| `popitem()`      | `tuple`              | (3.6 이상) 마지막 `(키, 값)` 튜플을 반환한다. | 25.1.5 |
| `clear()`        |                      | 딕셔너리의 모든 키-값 쌍을 삭제한다.          | 25.1.6 |
| `get(키)`        | `키`에 해당하는 `값` | 딕셔너리에서 `키`에 해당하는 `값`을 반환한다. | 25.1.7 |
| `items()`        | `dict_items`         | 딕셔너리의 모든 키-값 쌍을 가져온다.          | 25.1.8 |
| `keys()`         | `dict_keys`          | 딕셔너리의 모든 키를 가져온다.                |        |
| `values()`       | `dict_values`        | 딕셔너리의 모든 값을 가져온다.                |        |
| `copy()`         | `dict`               | 딕셔너리를 복사하여 새로운 객체를 반환한다.   | 25.5   |



## dict class Method

| 함수                            | 반환   | 기능                                                         | 기능   |
| ------------------------------- | ------ | ------------------------------------------------------------ | ------ |
| `dict.fromkeys(리스트)`         | `dict` | `리스트`의 요소들을 키로, 각각의 키에 대한 값은 `None`으로 저장한 딕셔너리를 반환한다. | 25.1.9 |
| `dict.fromkeys(리스트, 기본값)` | `dict` | `리스트`의 요소들을 키로, 각각의 키에 대한 값은 `기본값`으로 저장한 딕셔너리를 반환한다. |        |



## collections class Method

| 함수                                      | 반환                      | 기능                                                         | 참고               |
| ----------------------------------------- | ------------------------- | ------------------------------------------------------------ | ------------------ |
| `collections.defaultdict(기본값반환함수)` | `collections.defaultdict` | 이 함수가 반환한 딕셔너리에 대하여, 존재하지 않는 키에 접근하여도 `기본값반환함수`가 반환하는 기본값이 반환된다. | 25.1.10<br />E08.1 |
| `collections.Counter(리스트)`             | `collections.Counter`     | 시퀀스 객체의 각각의 항목을 키로, 해당 항목의 개수를 값으로 짝지은 딕셔너리를 반환한다. | E08.2              |
| `collections.deque(반복가능한객체)`       | `collections.deque`       | `반복가능한객체`를 새로운 `deque` 객체로 반환한다.           | E10.1              |



## collections.deque

| 함수                         | 반환         | 기능                                                         | 참고 |
| ---------------------------- | ------------ | ------------------------------------------------------------ | ---- |
| `append(요소)`               |              | 데크의 오른쪽에 `요소` 추가                                  | 10.2 |
| `appendleft(요소)`           |              | 데크의 왼쪽에 `요소` 추가                                    |      |
| `clear()`                    |              | 데크에서 모든 요소를 삭제                                    |      |
| `pop()`                      | `마지막요소` | 데크의 오른쪽에서 요소를 제거하고 반환<br />요소가 없으면 `IndexError` 발생 | 10.4 |
| `popleft()`                  | `첫번째요소` | 데크의 왼쪽에서 요소를 제거하고 반환<br />요소가 없으면 `IndexError` 발생 |      |
| `remove(요소)`               |              | 데크에서 `요소`를 삭제한다.<br />요소가 없으면 `ValueError`  |      |
| `extend(반복가능한객체)`     |              | 데크의 오른쪽에 `반복가능한객체`의 요소들을 추가한다.        | 10.5 |
| `extendleft(반복가능한객체)` |              | 데크의 왼쪽에 `반복가능한객체`의 요소들을 추가한다.<br />추가된 요소들의 순서는 원본의 순서를 뒤집은 것이다. |      |
| `len(데크)`                  | `int`        | `데크`의 요소의 길이를 반환한다.                             | 10.6 |
| `copy()`                     |              | 데크의 얕은 복사(객체의 주소 복사)                           |      |
| `copy.copy(데크)`            |              | 데크의 얕은 복사                                             |      |
| `copy.deepcopy(데크)`        |              | 데크의 깊은 복사                                             |      |
| `count(요소)`                | `int`        | 데크에 있는 `요소`의 개수를 반환한다.                        |      |
| `index(요소)`                | `int`        | `요소`의 인덱스를 반환한다.<br />`요소`를 찾지 못하면 `ValueError`를 발생시킨다. |      |
| `reverse()`                  |              | 원본 객체의 순서를 뒤집고 `None`을 반환한다.                 |      |
| `reversed(데크)`             |              | `데크`의 순서를 뒤집어 새로운 `reversed` 객체를 반환한다.    |      |
| `rotate(n=1)`                |              | 데크를 `n`만큼 오른쪽으로 회전한다. 따라서 `n`이 음수이면 왼쪽으로 회전한다.<br />데크가 비어 있지 않으면 오른쪽으로 한 단계 회전하는 것은 `d.appendleft(d.pop())`와 같다.<br />왼쪽으로 한 단계 회전하는 것은 `d.append(d.popleft())`와 같다. |      |





## copy class Method

| 함수                      | 반환   | 기능                                               | 참고   |
| ------------------------- | ------ | -------------------------------------------------- | ------ |
| `copy.deepcopy(딕셔너리)` | `dict` | `딕셔너리`를 깊은 복사하여 새로운 객체를 반환한다. | 25.5.1 |



## set Method

| 함수                                     | 반환   | 연산자 | 기능                                                         | 참고   |
| ---------------------------------------- | ------ | ------ | ------------------------------------------------------------ | ------ |
| `set.union(세트1, 세트2)`                | `set`  | `|`    | 합집합                                                       | 26.2   |
| `set.intersection(세트1, 세트2)`         | `set`  | `&`    | 교집합                                                       |        |
| `set.difference(세트1, 세트2)`           | `set`  | `-`    | 차집합                                                       |        |
| `set.symmetric_differnece(세트1, 세트2)` | `set`  | `^`    | 대칭 차집합                                                  |        |
| `update(세트)`                           | `set`  | `|=`   | 합집합 할당 연산                                             | 26.2.1 |
| `intersection_update(세트)`              | `set`  | `&=`   | 교집합 할당 연산                                             |        |
| `difference_update(세트)`                | `set`  | `-=`   | 차집합 할당 연산                                             |        |
| `symmetric_differnece_update(세트)`      | `set`  | `^=`   | 대칭 차집합 할당 연산                                        |        |
| `issubset(세트)`                         | `bool` | `<=`   | 세트 객체가 `세트`의 부분집합인지 확인한다.                  | 26.2.2 |
|                                          | `bool` | `<`    | 세트 객체가 `세트`의 진부분집합인지 확인한다.                |        |
| `issuperset(세트)`                       | `bool` | `>=`   | 세트 객체가 `세트`의 상위집합인지 확인한다.                  |        |
|                                          | `bool` | `>`    | 세트 객체가 `세트`의 진상위집합인지 확인한다.                |        |
| `isdisjoint(세트)`                       | `bool` |        | 세트 객체가 `세트`와 교집합을 가지는지 확인한다.             | 26.2.4 |
| `add(값)`                                |        |        | 세트에 `값`을 추가한다.                                      | 26.3   |
| `remove(값)`                             |        |        | 세트에서 `값`을 삭제한다.<br />해당하는 `값`이 없으면 `KeyError`를 발생시킨다. |        |
| `discard(값)`                            |        |        | 세트에서 `값`을 삭제한다.<br />해당하는 `값`이 없어도 그냥 넘어간다. |        |
| `pop()`                                  | `값`   |        | 세트에서 임의의 값을 삭제하고 해당 `값`을 반환한다.<br />세트에 요소가 없다면, `KeyError: 'pop from an empty set'`를 발생시킨다. |        |
| `clear()`                                |        |        | 세트에서 모든 요소를 삭제한다.                               |        |
| `len(세트)`                              | `int`  |        | `세트`의 길이를 구하여 반환한다.                             |        |
| `copy(세트)`                             | `set`  |        | 세트 객체를 복사하여 새로운 객체를 반환한다. (깊은 복사)     |        |



## functools

| 함수                           | 반환 | 설명                                                         | 참고   |
| ------------------------------ | ---- | ------------------------------------------------------------ | ------ |
| `reduce(함수, 반복가능한객체)` |      | `반복가능객체`의 각 요소를 `함수`로 처리한 뒤 이전 결과와 누적해서 반환한다. | 33.2.4 |



## re

| 함수                                         | 반환          | 설명                                                         | 참고   |
| -------------------------------------------- | ------------- | ------------------------------------------------------------ | ------ |
| `re.match(패턴, 문자열)`                     | `re.Match`    | `문자열`의 **시작 부분**이 정규식 `패턴`과 일치하면, 해당하는 일치 객체를 반환한다. 그렇지 않다면 `None`을 반환한다. | 43.1   |
| `re.search(패턴, 문자열)`                    | `re.Match`    | `문자열`의 일부분이 정규식 `패턴`과 일치하면, 해당하는 일치 객체를 반환한다. 그렇지 않다면 `None`을 반환한다. | 43.1.1 |
| ` re.compile('패턴')`                        | `re.Pattern`  | 정규표현식 패턴을 객체로 만든다.                             | 43.2.7 |
| `매치객체.group(그룹인덱스)`                 | `str | tuple` | 단일 인자에 대하여 단일 문자열을, 여러 개의 인자에 대하여는 인자당 하나의 항목을 가진 튜플을 반환한다. | 43.3   |
| `매치객체.group(그룹이름)`                   | `str`         | `그룹이름`으로 매칭된 문자열 반환한다.                       |        |
| `매치객체.groups()`                          | `tuple`       | 각 그룹에 매칭된 문자열을 `tuple`로 반환한다.                |        |
| `re.findall(패턴, 문자열)`                   | `list`        | `패턴`에 매칭된 문자열을 리스트로 반환한다.                  | 43.3.1 |
| `re.sub(패턴, 바꿀문자열, 문자열, 바꿀횟수)` | `str`         | `문자열`에서 `패턴`에 일치하는 문자열을 찾아 `바꿀문자열`로 `바꿀횟수`만큼 바꾼 문자열을 반환한다. `바꿀횟수`를 생략하면 찾은 모든 문자열을 바꾼다. | 43.4   |
| `re.sub(패턴, 함수, 문자열, 바꿀횟수)`       | `str`         | `문자열`에서 `패턴`에 일치하는 문자열을 찾아 `바꿀문자열`로 `바꿀횟수`만큼 바꾼 문자열을 반환한다. `바꿀횟수`를 생략하면 찾은 모든 문자열을 바꾼다. |        |

